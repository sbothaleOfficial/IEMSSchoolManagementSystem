<Window x:Class="IEMS.WPF.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset Password" Width="500"
        SizeToContent="Height" MinHeight="360" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SurfaceBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔑" FontSize="32" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Reset Password" FontSize="24" FontWeight="Bold"
                               Foreground="{StaticResource TextOnPrimaryBrush}"/>
                    <TextBlock Text="Enter new password for the user" FontSize="12"
                               Foreground="{StaticResource PrimaryLightBrush}" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Form Content -->
        <StackPanel Grid.Row="1" Margin="30">
            <!-- New Password -->
            <TextBlock Text="New Password *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtNewPassword" Height="35" Margin="0,0,0,5"/>
            <TextBlock Text="Must be 8+ characters with uppercase, lowercase, number, and special character"
                       FontSize="11" TextWrapping="Wrap"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,15"/>

            <!-- Confirm Password -->
            <TextBlock Text="Confirm Password *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtConfirmPassword" Height="35" Margin="0,0,0,15"/>

            <!-- Error Message -->
            <TextBlock x:Name="txtError" Text="" Foreground="Red" TextWrapping="Wrap"
                       Visibility="Collapsed"/>
        </StackPanel>

        <!-- Action Buttons -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnReset" Content="🔑 Reset Password" Click="BtnReset_Click"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Width="150" Height="40" Margin="0,0,10,0"/>
                <Button x:Name="btnCancel" Content="✕ Cancel" Click="BtnCancel_Click"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="120" Height="40"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>